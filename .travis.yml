sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t samadefemi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true samadefemi/docker-react npm run test
